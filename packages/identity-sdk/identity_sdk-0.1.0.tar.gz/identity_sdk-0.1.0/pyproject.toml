[tool.poetry]
name = "identity-sdk"
version = "0.1.0"
description = "The Python SDK for The Hacker App's identity platform"
authors = ["Alex Krantz <alex@krantz.dev>"]
readme = "README.md"
packages = [{include = "identity_sdk"}]

[tool.poetry.dependencies]
python = "^3.10"
starlite = "^1.45.1"


[tool.poetry.group.dev.dependencies]
black = "^22.12.0"
isort = "^5.11.3"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
