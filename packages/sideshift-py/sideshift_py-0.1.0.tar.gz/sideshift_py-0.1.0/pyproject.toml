[tool.poetry]
name = "sideshift-py"
version = "0.1.0"
description = ""
authors = ["bart"]
license = "mit"
readme = "README.md"
packages = [{include = "sideshift_py"}]

[tool.poetry.dependencies]
python = "^3.11"
requests = "^2.28.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
