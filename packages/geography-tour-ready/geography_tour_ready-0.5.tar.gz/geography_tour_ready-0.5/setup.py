from setuptools import setup

setup(name='geography_tour_ready',
      version='0.5',
      description= 'You can explore',
      author='Divyanshi',
      packages=['geography_tour_ready'],
      zip_safe=False)