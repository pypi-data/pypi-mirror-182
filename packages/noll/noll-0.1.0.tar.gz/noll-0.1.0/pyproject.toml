[tool.poetry]
name = "noll"
version = "0.1.0"
description = "Nothing. Nil. Nada."
readme = "README.md"
authors = ["Maximillian Strand <maxi@millian.se>"]
repository = "https://gitlab.com/deepadmax/noll"

[tool.poetry.dependencies]
python = "^3.9"

[tool.poetry.dev-dependencies]
pytest = "^5.2"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
