[tool.poetry]
name = "newcls"
version = "0.1.0"
description = "A basic clear disc."
authors = ["Gabs "]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
