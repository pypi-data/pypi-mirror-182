[tool.poetry]
name = "txSweeper"
version = "0.3.0"
description = "An infinitely generative minesweeper game."
authors = ["k01e <k01e.alm07@gmail.com>"]
license = "MIT"

[tool.poetry.dependencies]
python = "^3.10"
textual = "^0.6.0"

[tool.poetry.scripts]
txSweeper = "txsweeper.main:main"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
