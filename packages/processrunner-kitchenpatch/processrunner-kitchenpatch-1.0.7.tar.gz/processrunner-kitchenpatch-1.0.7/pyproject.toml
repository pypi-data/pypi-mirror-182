[project]
name = "processrunner-kitchenpatch"
version = "1.0.7"
dynamic = ["dependencies"]
description = "Patched getwriter method for Kitchen package"
readme = "README.rst"
authors = [{ name = "Andy Robb", email = "andy@andyrobb.com" }]
license = { file = "COPYING" }
keywords = ["utility"]
classifiers = [
    "Intended Audience :: Developers",
    "Topic :: Utilities",
    "License :: OSI Approved :: GNU Library or Lesser General Public License (LGPL)",
    "Topic :: Software Development :: Libraries :: Python Modules"
]

[project.urls]
homepage = "https://github.com/arobb/python-processrunner-kitchenpatch"
repository = "https://github.com/arobb/python-processrunner-kitchenpatch"

[build-system]
requires = [
    "setuptools == 44 ; python_version < '3'",
    "setuptools >= 45 ; python_version >= '3'",
    "setuptools_scm[toml] >= 5 ; python_version < '3'",
    "setuptools_scm[toml] >= 6.2 ; python_version >= '3'",
    "wheel >= 0.29.0",
]
build-backend = 'setuptools.build_meta'

[tool.setuptools_scm]

