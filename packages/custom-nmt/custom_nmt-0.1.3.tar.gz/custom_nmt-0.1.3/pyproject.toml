[tool.poetry]
name = "custom-nmt"
version = "0.1.3"
description = ""
authors = ["Your Name <you@example.com>"]
readme = "README.md"
packages = [{include = "custom_nmt"}]

[tool.poetry.dependencies]
python = "^3.9"
tensorflow = "^2.11.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
