[tool.poetry]
name = "nowcl"
version = "0.1.0"
description = "now"
authors = ["N"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
